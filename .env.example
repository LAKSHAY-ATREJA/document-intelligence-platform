# Document Intelligence Platform — Environment Variables
# Copy this file to .env and fill in your values.
# The application reads GROQ_API_KEY at startup and pre-fills the API key
# field in the sidebar, which is useful for server deployments.

# Required — obtain a free key from https://console.groq.com
GROQ_API_KEY=gsk_your_key_here
